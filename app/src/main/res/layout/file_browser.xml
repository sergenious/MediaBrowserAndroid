<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

	<LinearLayout
		android:id="@+id/fileBrowserEmpty"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:orientation="vertical">
		<TextView
			android:id="@+id/fileBrowserEmptyLabel"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:gravity="center_horizontal"
			android:text="@string/loading"
			android:textColor="#ffffffff"
			android:textSize="16sp"
			android:padding="16dip" />
		<ImageView
			android:id="@+id/fileBrowserEmptyIcon"
			android:layout_width="30dp"
			android:layout_height="30dp"
			android:layout_gravity="center_horizontal"
			android:src="@drawable/ic_loading"
			android:contentDescription="@string/loading"/>
	</LinearLayout>

	<GridView
	    android:id="@+id/fileBrowserList"
	    android:layout_width="fill_parent"
	    android:layout_height="fill_parent"
		android:drawSelectorOnTop="true"
		android:scrollbarTrackVertical="@drawable/scrollbar"
		android:scrollbarSize="8dip" />

</LinearLayout>
